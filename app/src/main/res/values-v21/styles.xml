<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme.movie_info_fragment">
        <item name="android:windowSharedElementEnterTransition">@transition/poster_shared_element</item>
        <item name="android:windowEnterTransition">@android:transition/fade</item>
        <item name="android:windowReturnTransition">@android:transition/fade</item>
        <item name="android:windowExitTransition">@android:transition/fade</item>

    </style>

    <style name="AppTheme.browse_fragment">
        <item name="android:windowExitTransition">@android:transition/fade</item>
        <item name="android:windowReenterTransition">@transition/browse_reenter</item>
    </style>

    <style name="AppTheme.cast_details_fragment">
        <item name="android:windowSharedElementEnterTransition">@transition/cast_shared_element</item>
        <item name="android:windowEnterTransition">@android:transition/fade</item>
        <item name="android:windowReturnTransition">@android:transition/fade</item>
    </style>




</resources>